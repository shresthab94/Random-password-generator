# Random-password-generator

This simple program allows user to genrate random passwords. The only criteria is the number of characters required has to be entered by the user. The program can be handy in today's world where a person needs to have numerous passwords to access different websites and applications.

The program uses simple 'Random' library. 
